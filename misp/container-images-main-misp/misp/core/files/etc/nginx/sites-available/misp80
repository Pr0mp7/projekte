server {
    listen 8080 default_server;
    listen [::]:8080 default_server;

    # disable access logs
    # access_log /var/www/MISP/app/tmp/logs/access.log;
    # log_not_found off;
    # error_log  /var/www/MISP/app/tmp/logs/error.log;

    # include includes/misp;
    # return 301 https://$host$request_uri;
}
